# Top-level runtime dependencies, shared by the Docker image and the native
# macOS launcher. The Dockerfile installs the torch pin below from the
# CPU-only PyTorch index first, so keep it as an exact "torch==X.Y.Z" line.
torch==2.14.0
kokoro==0.9.4
numpy==2.4.6
soundfile==0.14.0

# spaCy English model used by Kokoro's G2P. Installed up front so Kokoro
# doesn't download it at runtime on every fresh container.
en_core_web_sm @ https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl#sha256=1932429db727d4bff3deed6b34cfc05df17794f4a52eeb26cf8928f7c1a0fb85
